@page "/Demo4"

<h1>Demo4 - Ref</h1>

<DemoComponent @ref="demoComponent" />
<span @ref="spanElement" />

@code {

	private DemoComponent demoComponent;
	private ElementReference spanElement;

	[Inject] ILogger<Demo4> Logger { get; set; }

	protected override void OnInitialized()
	{
		Logger.LogInformation(nameof(OnInitialized));
		Logger.LogInformation(demoComponent == null ? "demoComponent == null" : "demoComponent != null");
		base.OnInitialized();
	}

	protected override void OnParametersSet()
	{
		Logger.LogInformation(nameof(OnParametersSet));
		Logger.LogInformation(demoComponent == null ? "demoComponent == null" : "demoComponent != null");
		base.OnParametersSet();
	}

	protected override void OnAfterRender(bool firstRender)
	{
		Logger.LogInformation(nameof(OnAfterRender));
		Logger.LogInformation(demoComponent == null ? "demoComponent == null" : "demoComponent != null");
		base.OnAfterRender(firstRender);
	}

}